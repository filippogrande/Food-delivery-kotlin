<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Kotlinfooddelivery" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Brand colors -->
        <item name="colorPrimary">@color/brand_orange</item>
        <item name="colorPrimaryVariant">@color/brand_orange</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Background general -->
        <item name="android:colorBackground">@color/white</item>
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Buttons default style -->
        <item name="buttonStyle">@style/AppButton</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Stile per i pulsanti principali (colore arancione pieno con testo bianco) -->
    <style name="AppButton" parent="Widget.AppCompat.Button">
        <item name="android:backgroundTint">@color/brand_orange</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>